# Backend API URL (required for production, set on Vercel)
VITE_API_URL=http://localhost:8080

# NOTE: VITE_YOUTUBE_API_KEY is DEPRECATED.
# The YouTube API key is now stored server-side only.
# Set YOUTUBE_API_KEY on your backend (Railway) environment instead.
